# 03 &mdash; Changing the DOM
> practising changes in the DOM

## Description

In the example, we illustrate how to rearrange the original paragraphs of the HTML document using `insertBefore()` method on a node.